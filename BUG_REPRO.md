# BUG_REPRO：关注关系方向错位

## Bug 是什么

关注/取关/计数的关系方向被反转：自己可以关注自己；取关后关系仍存在；粉丝数与关注数错乱。

## 如何触发

服务启动后调用 `POST /api/v1/users/:id/follow` 与 `DELETE /api/v1/users/:id/follow`，再查看个人主页计数。

## 真实错误信息

取关后粉丝数/关注数不变，关注自己返回成功（应为 422）。
